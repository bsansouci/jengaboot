{
  "name": "jengaboot",
  "version": "0.0.22",
  "description": "Prototype that conforms to the magic-build spec. Implemented with Jenga.",
  "main": "index.js",
  "dependencies": {
    "ocamlBetterErrors": ">=0.0.10"
  },
  "bin": {
    "run": "run",
    "compile": "compile"
  },
  "devDependencies": {},
  "scripts": {
    "postinstall": "ocamlfind ocamlc -pp refmt -linkpkg -package yojson -g -impl buildUtils/extractDeps.re -o buildUtils/extractDeps.out && ocamlfind ocamlc -pp refmt -linkpkg -package yojson -g -impl buildUtils/extractOcamlfindDeps.re -o buildUtils/extractOcamlfindDeps.out && ocamlfind ocamlc -pp refmt -linkpkg -package yojson -g -impl buildUtils/extractPpxFlag.re -o buildUtils/extractPpxFlag.out",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chenglou/jengaboot.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/chenglou/jengaboot/issues"
  },
  "homepage": "https://github.com/chenglou/jengaboot#readme"
}
